import React from "react";

import Navbar from "../Navbar";
import Carousel from "../Carousel";

const Header = () => {
  return (
    <home>
      <Navbar />
      <Carousel></Carousel>
    </home>
  );
};

export default Header;
